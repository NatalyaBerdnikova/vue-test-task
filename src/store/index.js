import Vuex from 'vuex';

import bin from './bin';
import catalog from './catalog';

export default new Vuex.Store({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    bin,
    catalog,
  },
});
